services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      DATABASE_URL: sqlite:///./data/reinfolib.db
    volumes:
      - ./data:/app/data
    restart: unless-stopped
